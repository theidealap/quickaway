Add a new tool called "TDEE Calculator" to the QuickAway site, following the existing tool patterns in src/tools/ (use mortgage-calculator.tsx or bmi-calculator.tsx as reference templates for structure and validation conventions).

STANDARD REQUIREMENTS (apply to every new tool, always):
1. Follow the exact UI/UX pattern of existing tools (hero card + secondary stat cards structure, same Tailwind classes and Radix UI components)
2. Client-side only — no backend calls, no external APIs
3. Input validation: handle empty fields, negative numbers where invalid, non-numeric input, inline error messages (not browser alerts)
4. Mobile responsive — test at 390px width equivalent
5. CRITICAL — learned from a previous bug: for any numeric input field, use explicit `=== ''` checks for the "is this field empty" guard, NEVER truthiness checks like `!value`, since a legitimately entered 0 must not be treated as empty
6. Add proper SEO: seoTitle, seoDescription in tools-registry.ts
7. Add the tool to src/lib/tools-registry.ts — create/use a "Health" category if one exists, otherwise group near bmi-calculator
8. Add an entry to public/sitemap.xml with lastmod 2026-07-29, changefreq monthly, priority 0.7
9. Do NOT modify any other existing tools, files, or shared components unless required
10. Add a "Related Tools" cross-link to BMI Calculator if the related-tools mechanism is registry-driven (same category or manual list — check how age-calculator/bmi-calculator currently link to related tools)
11. Run a self-check with a table covering all edge cases and confirm build is clean

THIS TOOL'S SPECIFIC REQUIREMENTS:
- Slug: tdee-calculator
- Name: "TDEE Calculator"
- Inputs:
  - Sex (toggle: Male / Female)
  - Age (years)
  - Height — toggle between cm and ft/in
  - Weight — toggle between kg and lb
  - Activity Level (dropdown, standard 5-tier scale):
    - Sedentary (little or no exercise) — multiplier 1.2
    - Lightly active (light exercise 1-3 days/week) — 1.375
    - Moderately active (moderate exercise 3-5 days/week) — 1.55
    - Very active (hard exercise 6-7 days/week) — 1.725
    - Extra active (very hard exercise, physical job) — 1.9
- Formula: Mifflin-St Jeor equation for BMR
  - Male: BMR = 10×weight(kg) + 6.25×height(cm) − 5×age + 5
  - Female: BMR = 10×weight(kg) + 6.25×height(cm) − 5×age − 161
  - TDEE = BMR × activity multiplier
- Output (hero card): TDEE (calories/day)
- Secondary stat cards: BMR, Maintenance Calories (same as TDEE, labeled for clarity), and a simple reference block showing Mild Weight Loss (TDEE − 250), Weight Loss (TDEE − 500), Weight Gain (TDEE + 500) as informational calorie reference points only — NOT a diet plan
- Validation: Age must be 15-100, Height 50-250cm equivalent, Weight 20-300kg equivalent, unit toggles must convert correctly between cm/ft-in and kg/lb without losing precision
- No breakdown table needed — single-calculation tool, keep it concise

CONTENT TONE: Keep all copy factual and neutral — no diet culture language, no "you should," no judgmental framing. Present numbers as informational reference points only. Include a brief disclaimer: "These are estimates for general reference. Consult a healthcare provider for personalized advice."

After making these changes, do NOT sync/push to GitHub yet — show me the file changes and the completed self-check table first, so I can review before deployment.